import 'package:flutter/material.dart';

class tab_holding extends StatefulWidget {
  @override
  State<tab_holding> createState() => _tab_holdingState();
}

class _tab_holdingState extends State<tab_holding> {
  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Text('dkm'));
  }
}
